<?xml version="1.0" encoding="utf-8"?>

<layout >

    <data>
        <variable
            name="myVariable"
            type="String" />

        <variable
            name="user"
            type="com.example.databindingjava.User" />

    </data>

    <LinearLayout xmlns:android ="http://schemas.android.com/apk/res/android"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        >

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{user.name, default = NoName}"
            android:id="@+id/name"
            android:textAppearance="@style/TextAppearance.AppCompat.Title"
            android:padding="8dp"
            />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{ String.valueOf(user.age), default = 00 }"
            android:id="@+id/age"
            android:textAppearance="@style/TextAppearance.AppCompat.Title"
            android:padding="8dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{user.active?`ACTIVE`:`InActive`, default = InActive}"
            android:id="@+id/active"
            android:textAppearance="@style/TextAppearance.AppCompat.Title"
            android:padding="8dp" />

        <Button
            android:text="@{user.name}"
            android:enabled="@{user.active}"
            android:onClick="@{() -> user.handleClicked()}"

            android:id="@+id/button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/Widget.AppCompat.Button.Colored"
            android:padding="8dp"
            />

        <ImageView
            android:whatever="@{user.imageUrl}"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:id="@+id/imageView"
            android:src="@drawable/ic_launcher_background"
            android:padding="8dp" />



    </LinearLayout>

</layout>